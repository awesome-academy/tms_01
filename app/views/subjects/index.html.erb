<% provide :title, t(".index_title") %>
<h1 class= "text-center"><%= t ".index_title" %></h1>
<%= link_to t(".create_subject"), new_subject_path, class: "btn btn-primary mb-3" %>

<%= will_paginate_helper @subjects %>

<div class="container-fluid">
  <div class="row">
    <%= render @subjects %>
  </div>

 <%= will_paginate_helper @subjects %>

</div>

